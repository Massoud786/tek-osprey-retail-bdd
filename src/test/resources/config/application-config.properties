#Commenting on Properties file

#Properties file base on key and value pairs
#key=value Naming convention for keys.ex ui url -> ui.url
#example: database host name -> database.host.name=http//localhost:
#example: database username -> database.username=myUsername

retail.ui.url = https://retail.tekschool-students.com/
retail.browser.type = chrome
retail.browser.headless = true
# headless will run the test in the background. It will not open the browser.
